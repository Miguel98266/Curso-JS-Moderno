let i = 0; //Iniciarlizar el while

while (i < 10) { //Condicion
 
  if (i % 2 == 0) {
    console.log(`Numero ${i} es par`);
  } else {
    console.log(`Numero ${i} es impar`);
  }
  i++; //Incremento
}
